spring:
  application:
    name: DirectNexus
  datasource:
    url: jdbc:postgresql://postgres:5432/direct_db
    username: keycloak
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081
